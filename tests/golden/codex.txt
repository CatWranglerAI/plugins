--- manage list --dir <WORK>/registry
{"ok":true,"exists":false,"path":"<WORK>/registry/.catwrangler","scope":"none","write_path":"<WORK>/registry/.catwrangler","server":null,"projects":[]}exit=0
--- manage add --slug solo --dir <WORK>/registry
{"ok":true,"action":"added","slug":"solo","ambiguous":false,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo"}]}exit=0
--- manage add --slug solo --use-when shared-plumbing --dir <WORK>/registry
{"ok":true,"action":"updated","slug":"solo","ambiguous":false,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing"}]}exit=0
--- manage add --slug solo --web-url https://solo-dev.catwrangler.ai --dir <WORK>/registry
{"ok":true,"action":"updated","slug":"solo","ambiguous":false,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai"}]}exit=0
--- manage add --slug solo --name SoloV2 --dir <WORK>/registry
{"ok":true,"action":"updated","slug":"solo","ambiguous":false,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"}]}exit=0
--- manage add --slug arcade --id p-841207 --org pixel --name Arcade --desc Cross-game. --web-url https://pixel-arcade-dev.catwrangler.ai --dir <WORK>/registry
{"ok":true,"action":"added","slug":"arcade","org_slug":"pixel","ambiguous":false,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"Arcade","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"}]}exit=0
--- manage add --slug arcade --org neon --dir <WORK>/registry
{"ok":true,"action":"added","slug":"arcade","org_slug":"neon","ambiguous":true,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"Arcade","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage add --slug arcade --org pixel --name ArcadeV2 --dir <WORK>/registry
{"ok":true,"action":"updated","slug":"arcade","org_slug":"pixel","ambiguous":true,"path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage list --dir <WORK>/registry
{"ok":true,"exists":true,"path":"<WORK>/registry/.catwrangler","scope":"cwd","write_path":"<WORK>/registry/.catwrangler","server":"https://mcp.catwrangler.ai","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage remove --slug arcade --dir <WORK>/registry
{"ok":false,"error":"slug \"arcade\" is registered in 2 orgs (pixel, neon) — pass --org to say which"}exit=1
--- manage remove --slug arcade --org neon --dir <WORK>/registry
{"ok":true,"action":"removed","slug":"arcade","org_slug":"neon","path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"}]}exit=0
--- manage remove --slug ghost --dir <WORK>/registry
{"ok":true,"action":"noop","slug":"ghost","path":"<WORK>/registry/.catwrangler","projects":[{"slug":"solo","use_when":"shared-plumbing","web_url":"https://solo-dev.catwrangler.ai","name":"SoloV2"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game.","web_url":"https://pixel-arcade-dev.catwrangler.ai"}]}exit=0
--- manage add --dir <WORK>/registry
{"ok":false,"error":"add requires --slug"}exit=1
--- manage frobnicate --dir <WORK>/registry
{"ok":false,"error":"unknown command: frobnicate — use list | add | remove"}exit=1
--- manage remove --slug x --dir <WORK>/nonexistent
{"ok":false,"error":"no .catwrangler in <WORK>/nonexistent"}exit=1
--- registry file
{
  "version": 1,
  "server": "https://mcp.catwrangler.ai",
  "mcp_url": "https://mcp.catwrangler.ai/mcp",
  "projects": [
    {
      "slug": "solo",
      "use_when": "shared-plumbing",
      "web_url": "https://solo-dev.catwrangler.ai",
      "name": "SoloV2"
    },
    {
      "slug": "arcade",
      "id": "p-841207",
      "org_slug": "pixel",
      "name": "ArcadeV2",
      "description": "Cross-game.",
      "web_url": "https://pixel-arcade-dev.catwrangler.ai"
    }
  ]
}
--- hunt list --dir <WORK>/hunt/repo/src/api
{"ok":true,"exists":true,"path":"<WORK>/hunt/repo/.catwrangler","scope":"ancestor","write_path":"<WORK>/hunt/repo/.catwrangler","server":null,"projects":[{"slug":"repo-proj","id":"p-500001"}]}exit=0
--- hunt add --slug deep --dir <WORK>/hunt/repo/src/api
{"ok":true,"action":"added","slug":"deep","ambiguous":false,"path":"<WORK>/hunt/repo/.catwrangler","projects":[{"slug":"repo-proj","id":"p-500001"},{"slug":"deep"}]}exit=0
--- hunt remove --slug deep --dir <WORK>/hunt/repo/src/api
{"ok":true,"action":"removed","slug":"deep","path":"<WORK>/hunt/repo/.catwrangler","projects":[{"slug":"repo-proj","id":"p-500001"}]}exit=0
--- hunt list --dir <WORK>/hunt/loose
{"ok":true,"exists":false,"path":"<WORK>/hunt/loose/.catwrangler","scope":"none","write_path":"<WORK>/hunt/loose/.catwrangler","server":null,"projects":[]}exit=0
--- hunt(home) list --dir <WORK>/hunt/loose
{"ok":true,"exists":true,"path":"<WORK>/home-reg/.catwrangler","scope":"home","write_path":"<WORK>/hunt/loose/.catwrangler","server":"https://mcp.catwrangler.ai","projects":[{"slug":"global","id":"p-000001","name":"Global"}]}exit=0
--- hunt(home) add --slug fromhome --dir <WORK>/hunt/loose
{"ok":true,"action":"added","slug":"fromhome","ambiguous":false,"path":"<WORK>/hunt/loose/.catwrangler","projects":[{"slug":"fromhome"}]}exit=0
--- hunt(home) remove --slug global --dir <WORK>/hunt/loose2
{"ok":false,"error":"no .catwrangler in <WORK>/hunt/loose2 — the projects in scope here come from <WORK>/home-reg/.catwrangler (your home registry). Run this from <WORK>/home-reg to disconnect one."}exit=1
--- hunt(home) list --dir <WORK>/home-reg
{"ok":true,"exists":true,"path":"<WORK>/home-reg/.catwrangler","scope":"cwd","write_path":"<WORK>/home-reg/.catwrangler","server":"https://mcp.catwrangler.ai","projects":[{"slug":"global","id":"p-000001","name":"Global"}]}exit=0
--- hunt(home) remove --slug global --dir <WORK>/home-reg
{"ok":true,"action":"removed","slug":"global","path":"<WORK>/home-reg/.catwrangler","projects":[]}exit=0
--- hunt files
<WORK>/hunt/loose/.catwrangler
<WORK>/hunt/repo/.catwrangler
--- hook none startup
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook none resume
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook none clear
{}exit=0
--- hook none compact
{}exit=0
--- hook none fork
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook one startup
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook one resume
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook one clear
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook one compact
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook one fork
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook many startup
{"systemMessage":"\n\nCatWrangler workspace: 5 projects connected (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook many resume
{"systemMessage":"\n\nCatWrangler workspace: 5 projects connected (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook many clear
{"systemMessage":"\n\nCatWrangler workspace: 5 projects connected (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook many compact
{"systemMessage":"\n\nCatWrangler workspace: 5 projects connected (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook many fork
{"systemMessage":"\n\nCatWrangler workspace: 5 projects connected (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook routed startup
{"systemMessage":"\n\nCatWrangler workspace: 3 projects connected (arcade, neon, plain).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207] — Cross-game plane.\n      use when: coins, accounts, leaderboards — anything shared across games\n      web: https://pixel-arcade-dev.catwrangler.ai\n  • neon [id p-773915]\n      use when: the racing game itself; they call it \"the racer\"\n      web: https://pixel-neon-dev.catwrangler.ai\n  • plain [id p-620384]\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook routed resume
{"systemMessage":"\n\nCatWrangler workspace: 3 projects connected (arcade, neon, plain).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207] — Cross-game plane.\n      use when: coins, accounts, leaderboards — anything shared across games\n      web: https://pixel-arcade-dev.catwrangler.ai\n  • neon [id p-773915]\n      use when: the racing game itself; they call it \"the racer\"\n      web: https://pixel-neon-dev.catwrangler.ai\n  • plain [id p-620384]\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook routed clear
{"systemMessage":"\n\nCatWrangler workspace: 3 projects connected (arcade, neon, plain).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207] — Cross-game plane.\n      use when: coins, accounts, leaderboards — anything shared across games\n      web: https://pixel-arcade-dev.catwrangler.ai\n  • neon [id p-773915]\n      use when: the racing game itself; they call it \"the racer\"\n      web: https://pixel-neon-dev.catwrangler.ai\n  • plain [id p-620384]\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook routed compact
{"systemMessage":"\n\nCatWrangler workspace: 3 projects connected (arcade, neon, plain).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207] — Cross-game plane.\n      use when: coins, accounts, leaderboards — anything shared across games\n      web: https://pixel-arcade-dev.catwrangler.ai\n  • neon [id p-773915]\n      use when: the racing game itself; they call it \"the racer\"\n      web: https://pixel-neon-dev.catwrangler.ai\n  • plain [id p-620384]\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook routed fork
{"systemMessage":"\n\nCatWrangler workspace: 3 projects connected (arcade, neon, plain).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207] — Cross-game plane.\n      use when: coins, accounts, leaderboards — anything shared across games\n      web: https://pixel-arcade-dev.catwrangler.ai\n  • neon [id p-773915]\n      use when: the racing game itself; they call it \"the racer\"\n      web: https://pixel-neon-dev.catwrangler.ai\n  • plain [id p-620384]\nRoute by the `use when` lines first — they are this user's own statement of which project owns which work, and they outrank anything you would infer from a description. Call init_session for the project that fits, passing its `id` above.\nYou may hold MORE THAN ONE of these open at once: a session can init_session into several projects and keep a separate agent_id per server. When you do, route every subsequent call by these same rules and send it with the agent_id belonging to that project's server — never let a call for one project ride another's session.\nIf more than one project plausibly applies and it is genuinely ambiguous, ask the user which — do not guess. When they answer, or when they correct a choice you already made, make it stick: use the /catwrangler:connect skill to update that project's `use when` note. A correction you do not record is one they have to repeat next session.\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook zero startup
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook zero resume
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook zero clear
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook zero compact
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook zero fork
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook bad startup
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - <WORK>/fix/bad/.catwrangler\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad resume
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - <WORK>/fix/bad/.catwrangler\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad clear
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - <WORK>/fix/bad/.catwrangler\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad compact
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - <WORK>/fix/bad/.catwrangler\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad fork
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - <WORK>/fix/bad/.catwrangler\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook nested-subdir startup
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - From <WORK>/fix/nested/.catwrangler (a parent directory).\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\nThis came from `<WORK>/fix/nested/.catwrangler`, in a parent of the current directory — it governs every directory beneath it.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook nested-subdir clear
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - From <WORK>/fix/nested/.catwrangler (a parent directory).\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\nThis came from `<WORK>/fix/nested/.catwrangler`, in a parent of the current directory — it governs every directory beneath it.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook home-fallback startup
{"systemMessage":"\n\nCatWrangler workspace detected (https://mcp.catwrangler.ai).\n  - From <WORK>/home-reg/.catwrangler — your home registry, used where no project claims the directory.\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\nThis came from `<WORK>/home-reg/.catwrangler`, the user's home registry — the fallback for directories no project claims. It applies because nothing between here and the filesystem root has its own `.catwrangler`. Adding a project from here creates one in the current directory rather than editing the home file.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook home-fallback clear
{"systemMessage":"\n\nCatWrangler workspace detected (https://mcp.catwrangler.ai).\n  - From <WORK>/home-reg/.catwrangler — your home registry, used where no project claims the directory.\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\n.catwrangler names no projects. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\nThis came from `<WORK>/home-reg/.catwrangler`, the user's home registry — the fallback for directories no project claims. It applies because nothing between here and the filesystem root has its own `.catwrangler`. Adding a project from here creates one in the current directory rather than editing the home file.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook nearest-wins startup
{"systemMessage":"\n\nCatWrangler workspace: 1 project connected (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to connect or disconnect projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`) — Cross-game plane. Call init_session for it with that id.\n  web: https://pixel-arcade-dev.catwrangler.ai\nA `web:` line is that project's CatWrangler UI and HTTP API — the same instance you reach over MCP, seen from a browser. It does not change how you work: the MCP tools remain the way you read code, decisions, and history, because they are the path through this project's gates. Use the URL to hand the user a link when they want to look at something themselves, or to reach an HTTP endpoint when a task genuinely needs one and no MCP tool covers it. It is not a second route to the project's contents, and it is never a reason to skip init_session.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nWhatever init_session returns is the authority on how to work this project — read it and follow it, even where it goes beyond these lines.\nThe workspace's `.catwrangler` file is a convenience cache, not the source of truth. If the user references a project this workspace is not connected to, call init_session to get the authoritative, current list from the server.\nConnecting is automatic and persistent: every future session started in this directory connects on its own at startup, with no /catwrangler:connect and no other setup step. Do not let the user believe otherwise — if they ask whether they need to connect, or reach for the command out of habit, tell them plainly that they are already correctly configured. The command is for connecting or disconnecting projects; re-running it is harmless, just unnecessary."}}exit=0
--- hook empty stdin
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook garbage stdin
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
