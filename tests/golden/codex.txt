--- manage list --dir <WORK>/registry
{"ok":true,"exists":false,"path":"<WORK>/registry/.catwrangler","server":null,"projects":[]}exit=0
--- manage add --slug solo --dir <WORK>/registry
{"ok":true,"action":"added","slug":"solo","ambiguous":false,"projects":[{"slug":"solo"}]}exit=0
--- manage add --slug arcade --id p-841207 --org pixel --name Arcade --desc Cross-game. --dir <WORK>/registry
{"ok":true,"action":"added","slug":"arcade","org_slug":"pixel","ambiguous":false,"projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"Arcade","description":"Cross-game."}]}exit=0
--- manage add --slug arcade --org neon --dir <WORK>/registry
{"ok":true,"action":"added","slug":"arcade","org_slug":"neon","ambiguous":true,"projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"Arcade","description":"Cross-game."},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage add --slug arcade --org pixel --name ArcadeV2 --dir <WORK>/registry
{"ok":true,"action":"updated","slug":"arcade","org_slug":"pixel","ambiguous":true,"projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game."},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage list --dir <WORK>/registry
{"ok":true,"exists":true,"path":"<WORK>/registry/.catwrangler","server":"https://mcp.catwrangler.ai","projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game."},{"slug":"arcade","org_slug":"neon"}]}exit=0
--- manage remove --slug arcade --dir <WORK>/registry
{"ok":false,"error":"slug \"arcade\" is registered in 2 orgs (pixel, neon) — pass --org to say which"}exit=1
--- manage remove --slug arcade --org neon --dir <WORK>/registry
{"ok":true,"action":"removed","slug":"arcade","org_slug":"neon","projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game."}]}exit=0
--- manage remove --slug ghost --dir <WORK>/registry
{"ok":true,"action":"noop","slug":"ghost","projects":[{"slug":"solo"},{"slug":"arcade","id":"p-841207","org_slug":"pixel","name":"ArcadeV2","description":"Cross-game."}]}exit=0
--- manage add --dir <WORK>/registry
{"ok":false,"error":"add requires --slug"}exit=1
--- manage frobnicate --dir <WORK>/registry
{"ok":false,"error":"unknown command: frobnicate — use list | add | remove"}exit=1
--- manage remove --slug x --dir <WORK>/nonexistent
{"ok":false,"error":"no .catwrangler in <WORK>/nonexistent"}exit=1
--- registry file
{
  "version": 1,
  "server": "https://mcp.catwrangler.ai",
  "mcp_url": "https://mcp.catwrangler.ai/mcp",
  "projects": [
    {
      "slug": "solo"
    },
    {
      "slug": "arcade",
      "id": "p-841207",
      "org_slug": "pixel",
      "name": "ArcadeV2",
      "description": "Cross-game."
    }
  ]
}
--- hook none startup
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook none resume
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook none clear
{}exit=0
--- hook none compact
{}exit=0
--- hook none fork
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook one startup
{"systemMessage":"\n\nCatWrangler workspace: 1 project available (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`). Call init_session for it with that id.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook one resume
{"systemMessage":"\n\nCatWrangler workspace: 1 project available (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`). Call init_session for it with that id.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook one clear
{"systemMessage":"\n\nCatWrangler workspace: 1 project available (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`). Call init_session for it with that id.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook one compact
{"systemMessage":"\n\nCatWrangler workspace: 1 project available (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`). Call init_session for it with that id.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook one fork
{"systemMessage":"\n\nCatWrangler workspace: 1 project available (arcade).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nOne project is configured here: **arcade** (Arcade Platform) (id `p-841207`). Call init_session for it with that id.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook many startup
{"systemMessage":"\n\nCatWrangler workspace: 5 projects available (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nPick the project that matches the user's task and call init_session for it, passing its `id` above. If more than one plausibly applies and it is ambiguous, ask the user which — do not guess.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook many resume
{"systemMessage":"\n\nCatWrangler workspace: 5 projects available (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nPick the project that matches the user's task and call init_session for it, passing its `id` above. If more than one plausibly applies and it is ambiguous, ask the user which — do not guess.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook many clear
{"systemMessage":"\n\nCatWrangler workspace: 5 projects available (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nPick the project that matches the user's task and call init_session for it, passing its `id` above. If more than one plausibly applies and it is ambiguous, ask the user which — do not guess.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook many compact
{"systemMessage":"\n\nCatWrangler workspace: 5 projects available (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nPick the project that matches the user's task and call init_session for it, passing its `id` above. If more than one plausibly applies and it is ambiguous, ask the user which — do not guess.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook many fork
{"systemMessage":"\n\nCatWrangler workspace: 5 projects available (arcade, neon, dungeon, d4, +1 more).\n  - Connecting via https://mcp.catwrangler.ai.\n  - Run /catwrangler:connect to manage projects.","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nProjects reachable in this workspace:\n  • arcade (Arcade) [id p-841207]\n  • neon [id p-773915]\n  • dungeon — cats\n  • d4\n  • d5\nPick the project that matches the user's task and call init_session for it, passing its `id` above. If more than one plausibly applies and it is ambiguous, ask the user which — do not guess.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook zero startup
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nNo projects are registered in .catwrangler. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook zero resume
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nNo projects are registered in .catwrangler. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook zero clear
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nNo projects are registered in .catwrangler. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook zero compact
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nNo projects are registered in .catwrangler. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook zero fork
{"systemMessage":"\n\nCatWrangler workspace detected (the CatWrangler MCP server).\n  - Retrieving your projects…","hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"This is a CatWrangler workspace. Before doing ANY work on the project, call the `catwrangler` MCP server's `init_session` tool. It returns your full working protocol and context — follow what it returns.\nWhen the project you connect to carries an `id` below, pass that id to init_session as its `project_id` parameter — it pins the exact project. Only fall back to the slug when no id is recorded.\nNo projects are registered in .catwrangler. Call init_session to retrieve the authoritative list of projects this user can reach, then proceed.\ninit_session returns an `agent_id`. Remember it, and include it as `_agent_id: \"<agent_id>\"` in the body of EVERY subsequent call to this server — calls without it are rejected. Each CatWrangler instance you connect to issues its OWN agent_id; use the matching one per server and never reuse one instance's agent_id on another. After an AUTH_REQUIRED error or a reconnect, call init_session with `reclaim_agent_id: \"<agent_id>\"` to recover without losing your branch or work — do not re-init without it.\nYou have NO local source code or decision files for the project — it all lives on the CatWrangler server behind its SCCS gates. Use the server's MCP tools (get_task_briefing, grep_code, read_code, list_files, search_decisions) for ALL code and decision access; do NOT use local file tools (Read, Grep, Glob, cat) to explore the project. The only local files are CLAUDE.md and Docs/.\nThis .catwrangler file is a convenience cache, not the source of truth. If the user references a project not registered here, call init_session to get the authoritative, current list from the server."}}exit=0
--- hook bad startup
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad resume
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad clear
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad compact
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook bad fork
{"systemMessage":"\n\nCatWrangler: found a .catwrangler file but could not parse it.\n  - Fix or regenerate it, or run /catwrangler:connect."}exit=0
--- hook empty stdin
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
--- hook garbage stdin
{"systemMessage":"\n\nCatWrangler: not connected to a project in this directory.\n  - Run /catwrangler:connect to see your projects and connect to one."}exit=0
